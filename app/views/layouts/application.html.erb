<!DOCTYPE html>
<html>
  <head>
    <title>RailsTvshowRecordings</title>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <!-- NOTE : Added for convenience -->
    <div class="container">
      <% if flash[:notice] %>
        <div class="notice"><%= flash[:notice] %></div>
      <% end %>
    </div>

    <!-- content -->
    <div class="container"  role="main">
      <%= yield %>
    </div>
    
  </body>
</html>
